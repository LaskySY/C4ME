spring:
  profiles:
    active: dev
  application:
    name: c4me-system
  boot:
    admin:
      client:
        url: http://localhost:8060

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8080/eureka

server:
  port: 3001